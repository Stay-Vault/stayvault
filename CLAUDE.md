# StayVault

Solanaを利用した留学生向け家賃エスクロー決済サービス。

日本の親は日本円で家賃を支払うだけでよく、
ブロックチェーン、USDC、ウォレットなどを意識せずに利用できるUXを目指す。

裏側では、円→USDCへの変換、Solana上でのエスクロー、
条件成立後の資金解放、USDC→AUDへの変換を行う。

対象はクイーンズランド州の民間学生寮・シェアハウス。
ボンドは州機関RTAの管轄のため、MVPでは扱わない。

本プロジェクトはColosseum Crypto World's Fair（提出期限 2026-10-12）への
提出物として開発する。評価軸は機能の広さではなく、
3分以内にプロダクトの価値が伝わるかどうかである。

## 固定バージョン（変更しない）

- Rust 1.90.0
- Solana CLI 4.1.2
- Anchor 1.2.0
- Node.js 22.23.2
- Yarn 1.22.22

このプロジェクトでは上記バージョンを使用する。

Claude Codeは、別バージョンを前提としたコードや設定を
勝手に採用しないこと。

バージョンを変更する場合は、実装前にチームへ変更内容を共有する。

## プロダクトの基本思想

ブロックチェーンや暗号資産をユーザーに意識させない。

### 親

- 日本円で支払う
- USDCを購入・保有・送金する必要はない
- Solanaウォレットを操作する必要はない
- 「日本円でいくら支払うか」を基本的なUIとして表示する
- 裏側で円→USDCへの変換を行う
- 裏側でSolana上のエスクローへ資金を入れる

### 学生

- 必要な契約情報・入居情報を確認できる
- 必要な操作のみを行う
- USDCやSolanaを意識しなくても利用できるUXを目指す

### 運営者

- 最終的にAUDで家賃を受け取る
- 裏側でUSDC→AUDへの変換を行う
- 運営者自身にSolanaやUSDCの操作を要求しないUXを目指す

## 資金の流れ

日本の親
    ↓
日本円で支払い
    ↓
StayVault
    ↓
裏側で円→USDC
    ↓
Solana
    ↓
エスクロー（PDA）
    ↓
条件成立
    ↓
運営者へUSDCを解放
    ↓
裏側でUSDC→AUD
    ↓
運営者がAUDで受け取る

ユーザーから見えるのは基本的に
「日本円で支払う → 家賃が条件に従って支払われる」
という流れとする。

## プロダクトの核

単にUSDCで家賃を支払えることが目的ではない。

核となる価値は以下。

- 資金をエスクローで保全する
- 条件に応じて資金を解放する
- 途中退去時に残額を親へ返金する
- 親が日本円で支払うだけで利用できる
- ブロックチェーンを裏側のインフラとして利用する

「USDCで払える」は差別化要素ではない。

### 約束（プロダクトが目指す状態）

入金済みの期間について、家賃滞納による是正通知（Form R11）が

発生しない状態を目指す。

### 検証方法

運営者が持つ是正通知の発行記録で測る。

「浪費が減った」のような主観的・間接的な指標では測らない。

機能追加やダッシュボード設計を行う際も、この検証方法に

沿った指標（是正通知の発行有無・件数）を基準とする。

なお、StayVaultがR11の発行そのものを直接制御・保証するものではない。

## MVPの前提

- Solana devnetを使用
- USDCを使用
- 実資金は扱わない
- 親が実際にUSDCを購入するフローは作らない
- 実際の円→USDCオンランプ接続はMVPでは実装しない
- 実際のUSDC→AUDオフランプ接続はMVPでは実装しない
- MVPではブロックチェーン上のUSDC決済フローをデモする
- 紛争処理はMVPでは実装しない
- 本人確認はMVPでは実装しない
- ボンドはMVPでは扱わない
- 実運用時の規制対応は別途ロードマップで扱う

## エスクロー設計

Vaultは秘密鍵を持たないPDAとして実装する。

運営者が資金を自由に動かせる設計にはしない。

以下を重要な設計原則とする。

- アップグレード権限を持たせない
- 管理者権限を作らない
- 返金先を契約作成時に親のアドレスで固定する
- 運営者が単独で資金を自由に移動できない
- 条件に基づいてプログラムが資金を管理する
- VaultはPDAとして扱う

設計変更を行う場合は、
非カストディという前提を壊していないか必ず確認する。

## コントラクトの主要命令

### fund

親の支払いに対応する資金を
エスクロー（PDA）へ入金する。

MVPではdevnet上のUSDCを使用する。

### confirm_move_in

学生＋運営者の2者署名で入居を確定し、
初月分を解放する。

運営者単独の署名では実行しない。

### release

支払日を過ぎていれば、誰でも実行できる。

対象となる1か月分を運営者へ解放する。

### terminate

親＋運営者で契約を終了する。

途中退去などの場合、
エスクローに残っている資金を親へ返金する。

### reclaim

期限内に入居確認がなければ、
親が残高を回収できる。

## 条件判定

条件判定に外部オラクルを使用しない。

契約内部で管理する情報を基準として判定する。

外部サービスの情報を直接信頼して
資金を解放する設計にはしない。

## 通貨

契約・エスクロー上ではUSDCを使用する。

ただし、ユーザー体験としては
親にUSDCを直接扱わせない。

親：
JPY → 裏側でUSDC

Solanaエスクロー：
USDC

運営者：
USDC → 裏側でAUD

という構造を基本とする。

### 為替に関する設計方針

- 契約金額はUSDC建てで固定する
- 円建て・AUD建ての金額は、契約時レートで換算して表示する
- 為替変動リスクの負担者（親／運営者のどちらが吸収するか）は
  定めない。契約時レートを固定することで、そもそも
  為替差の精算問題が発生しない設計とする
- レート変換ロジックを実装する際は、上記の
  「契約時レート固定」の前提を崩していないか確認する

## 開発方針

外側から作る。

先にデモ台本を固め、
そのデモを再生するために必要な機能だけを実装する。

機能の広さよりも、
3分以内にプロダクトの価値が伝わることを優先する。

契約1か月分の流れを60秒程度に短縮してデモできるようにする
（月次解放と返金を短時間で見せるため）。

## 開発スケジュール上の制約

- コントラクト（programs/）は **2026-10-05 で凍結**し、
  それ以降は変更しない
- 2026-10-05時点で fund → confirm_move_in → release → terminate
  の一連の流れがdevnet上で通しで動作しない場合、terminateの
  返金処理はモック表示に切り替える（コントラクト側のロジックを
  変更するのではなく、フロント側の表示切替で対応する）
- 2026-10-11 は予備日として確保し、新機能の追加は行わない
- 提出期限は2026-10-12

上記の期日・凍結ルールに反する変更（凍結後のprograms/変更、
予備日での新機能追加など）を行う場合は、
実装前に必ずチームへ確認する。

## 検討中の追加要件（未確定・要チーム確認）

以下はSlack上の資料で言及されているが、MVPスコープとして
正式に確定していない。実装前に必ずチームに確認すること。
CLAUDE.mdの「MVPで実装しない機能を勝手に追加しない」という
原則が優先される。

- **退去時の自動解約**：現状、契約解除は`terminate`（親＋運営者の
  2者署名）でのみ行われる。これとは別に「運営者が退去を登録したら
  自動的に契約を解約する」という案が検討されている。追加する場合は、
  運営者単独の操作で資金移動（残高の返金処理）が発動する設計に
  ならないよう、非カストディ・単独権限禁止の原則との整合性を
  必ず確認してから実装する。

## 規制対応（参考メモ）

実運用時の規制対応は別途ロードマップで扱うが、方向性として
以下を前提に説明する（MVPでの実装は行わない）。

- 非カストディ設計を維持する
- 日本側の資金移動業・仲介業に関する登録（2026年6月施行の
  規制を想定）を前提とする

## チーム分担

### なおや

- Anchorプログラム
- コントラクト命令
- テスト
- devnetデプロイ

### 恵悟

- フロントエンド
- ウォレット接続
- ダッシュボード
- UI設計
- 運営者ヒアリング
- ピッチ
- 動画

## ファイルの担当範囲

- programs/ → なおや（Anchor担当）
- app/ → 恵悟（フロント担当）
- docs/ → 2人で必要に応じて編集
- .github/workflows/ → CI/CD関連

担当範囲を越えて同じファイルを同時に編集しない。

不要なコンフリクトを避けるため、
基本的に担当ディレクトリを分けて作業する。

## Gitルール

- mainブランチに毎日マージする
- 作業内容は小さくコミットする
- 秘密鍵・ウォレット情報・APIキー・環境変数をコミットしない
- .gitignoreを尊重する
- 大きな変更を行う場合はチームに共有する
- コントラクトは2026-10-05で凍結し、以降はprograms/を変更しない
  （「開発スケジュール上の制約」を参照）

## セキュリティ

以下をGitHubへpushしない。

- 秘密鍵
- keypair.json
- wallet関連ファイル
- .env
- APIキー
- その他の認証情報

秘密情報をコード、README、CLAUDE.mdへ直接記載しない。

## UXの重要原則

ユーザーがブロックチェーンを使っていることを
必要以上に意識しない設計にする。

特に親については、

「USDCを購入してください」
「Solanaウォレットを接続してください」
「トークンを送信してください」

といった暗号資産前提の操作を要求しない。

ユーザーに必要な情報は、
基本的に日本円・契約期間・支払状況など、
通常の家賃決済サービスとして理解できる形で表示する。

ブロックチェーンはユーザー体験の裏側で
エスクローと条件付き資金解放を実現するインフラとして利用する。

## Claude Codeへの指示

このプロジェクトのコードを書く際は、
必ずCLAUDE.mdに記載されたバージョン・設計・役割分担を前提とする。

古いAnchorの書き方や別バージョンを前提とした実装を
勝手に採用しない。

実装前に既存ファイルとプロジェクト構造を確認する。

担当範囲外のファイルを変更する場合は、
変更理由を明示する。

大きな設計変更、バージョン変更、
権限モデルの変更を行う場合は、
実装前に変更点を明示する。

特にエスクロー、返金、資金解放、
権限管理に関わるコードは、
既存の設計原則を確認してから変更する。

ユーザーに暗号資産操作を要求する設計へ
勝手に変更しない。

MVPで実装しない機能を勝手に追加しない。
「検討中の追加要件（未確定・要チーム確認）」に記載された機能は、
チームの明示的な確認なしに実装しない。

2026-10-05以降はprograms/を変更しない
（「開発スケジュール上の制約」を参照）。

